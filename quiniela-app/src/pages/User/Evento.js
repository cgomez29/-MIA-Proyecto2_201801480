import React from 'react';
import {Grid} from "@material-ui/core";
import EventFullCalendar from "../../components/EventFullCalendar";

const Evento = () => {
    return (
            <EventFullCalendar/>
    );
};

export default Evento;